# Copy this to .env and fill in your keys — never commit .env

# Anthropic API key — get one at console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# NVD API key (optional but recommended — free at nvd.nist.gov/developers)
# Without this key requests are rate-limited to 5 req/30s
NVD_API_KEY=
