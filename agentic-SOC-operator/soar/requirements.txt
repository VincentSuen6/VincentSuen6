# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
slowapi>=0.1.9

# Message queue
pika>=1.3.2
celery[redis]>=5.3.6

# Cache / state
redis>=5.0.4

# HTTP client (VT + Splunk HEC)
httpx>=0.27.0

# Numeric + embeddings
numpy>=1.26.4
sentence-transformers>=3.0.0

# Vector store
qdrant-client>=1.9.0

# LangGraph workflow engine
langgraph>=0.1.0
langchain-core>=0.2.0

# PostgreSQL checkpointer (psycopg3 — not psycopg2)
psycopg[binary,pool]>=3.1.18
langgraph-checkpoint-postgres>=1.0.4

# OpenTelemetry — distributed tracing exported to Jaeger
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0

# Anthropic SDK — Sigma rule auto-generation in feedback.py
anthropic>=0.30.0

# Testing
pytest>=8.0.0
